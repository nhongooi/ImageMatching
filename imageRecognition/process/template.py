""" search through images if have a given template"""


def template_match(template, image_list):
    """ search through image_list and return images with
        image match with template"""
    pass